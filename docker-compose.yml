services:
  # Servicio de frontend (Angular)
  frontend:
    build:
      context: .  # Ruta a la carpeta de tu proyecto Angular
      dockerfile: Dockerfile
    container_name: talento-front
    ports:
    - "3201:80" 